Arbchat — Colab-ready lightweight build.

This package is configured to build in Google Colab without extra native recipes.
Bluetooth is simulated so the APK will run but won't use real Bluetooth yet.